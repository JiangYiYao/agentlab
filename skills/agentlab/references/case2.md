# Case 2 推荐关注点

独立 gate，不要一条 concern 两个 measure：

- sections-present
- counterarg-inline
- label-align
- no-upgrade-without-evidence
- output-path-invariant

latency / tokens 在 CI 合成契约上不要写 `pass`。
prompt 必须写 `TRADING_SKILL_DIR=${program_root}`。
隔离：homedir + 独立 SCUTIO_HOME + replay hook。
禁止提交用户私有 scutio-trading 或未发布茅台夹具。
